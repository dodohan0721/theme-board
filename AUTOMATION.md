# 맥 없이 자동으로 돌리기 — 설정 안내

지금은 맥에서 `export_snapshot.py`를 직접 돌려야 화면이 갱신됩니다.
아래대로 하면 **맥을 꺼놔도 장중에 10분마다 자동 갱신**됩니다.

---

## 왜 Vercel Cron 이 아니라 GitHub Actions 인가

| | Vercel Hobby | GitHub Actions |
|---|---|---|
| 실행 주기 | **하루 1회** (공식 문서 명시) | **5분 간격** 가능 |
| 실행 시간 제한 | 최대 60초 | **6시간** |
| 파이썬 코드 | 서버리스 함수로 재작성 필요 | **지금 코드 그대로** |
| 비용 | 무료 | 공개 저장소 무료 / 비공개 월 2,000분 |

Vercel Hobby 플랜은 크론이 **하루 한 번**으로 제한돼 있어 장중 갱신이 불가능합니다.
Pro($20/월)로 올리면 분 단위가 가능하지만, 그래도 **함수 60초 제한** 때문에
지금의 40초짜리 스캔 + AI 판독을 한 번에 넣기 빠듯합니다.

GitHub Actions 는 파이썬을 그대로 실행하므로 **코드를 한 줄도 안 고쳐도 됩니다.**

> Vercel 환경변수 자체는 사장님 말씀대로 안전합니다. 서버리스 함수에서만 읽히고
> 브라우저로는 안 나갑니다. 제가 "위험하다"고 한 건 과했습니다.
> 문제는 보안이 아니라 **크론 주기와 실행시간 제한**입니다.

---

## 설정 — 명령 한 줄

```bash
cd ~/Desktop/theme-board
bash setup_github.sh
```

이 스크립트가 알아서 합니다.

1. **`.gitignore` 생성** — `config.py`가 절대 올라가지 않도록 막고, 실제로 제외됐는지 검사까지 합니다
2. **워크플로 생성** — `.github/workflows/snapshot.yml`
3. **`config.py`에서 키를 읽어** 화면에 정리해 보여줍니다 (또는 `gh` CLI가 있으면 Secrets에 자동 등록)
4. **git 초기화 + 첫 커밋**

`gh` CLI가 설치돼 있고 로그인돼 있으면 **저장소 생성·업로드·Secrets 등록까지 전부 자동**입니다.
없으면 붙여넣을 값들을 표로 출력해 주니 GitHub 웹에서 등록하시면 됩니다.

### 직접 하셔야 하는 것 두 가지

**① Vercel 토큰 발급**

vercel.com → 우측 상단 아바타 → **Settings → Tokens → Create Token**
발급된 값을 `VERCEL_TOKEN` Secret으로 등록합니다.

**② 저장소를 공개(public)로**

공개면 Actions 실행 시간이 **무제한 무료**라 10분 간격으로 돌릴 수 있습니다.
`config.py`는 `.gitignore`로 막혀 있어 **키가 올라갈 일이 없습니다.**

### 확인

저장소 → **Actions** 탭 → `테마보드 스냅샷 갱신` → **Run workflow** 버튼.
초록불이 뜨면 끝입니다. 이후 평일 09:00~16:00에 10분마다 자동으로 돕니다.

---

## 실행 시간 계산 (비공개 저장소일 때)

무료 한도는 **월 2,000분**입니다.

| 주기 | 하루 실행 | 1회 소요 | 월 사용량 |
|---|---|---|---|
| 5분 | 78회 | 4분 | **6,900분** ❌ 초과 |
| 10분 | 39회 | 4분 | **3,400분** ❌ 초과 |
| 20분 | 20회 | 4분 | **1,760분** ✅ |
| 30분 | 13회 | 4분 | **1,150분** ✅ |

**공개 저장소면 무제한**이라 5분 간격도 가능합니다.
비공개로 가시려면 `snapshot.yml`의 `*/10` 을 `*/20` 또는 `*/30` 으로 바꾸세요.

`--cycles 2 --ai-top 30` 으로 이미 가볍게 잡아뒀습니다.
더 줄이려면 `--ai` 를 빼면 1분 안쪽으로 끝납니다.

---

## 더 깔끔한 방식 — R2 를 쓰면

지금 방식은 갱신할 때마다 **Vercel 전체를 재배포**합니다. 동작에는 문제없지만
배포 이력이 하루 39개씩 쌓입니다.

R2(사장님이 이미 계정 보유)를 쓰면 **`data.json` 한 개만 갈아끼우면** 됩니다.

```
GitHub Actions → data.json 을 R2 에 업로드
                      ↓
Vercel 에는 HTML 만 (한 번 배포하고 끝)
                      ↓
브라우저가 R2 주소에서 data.json 을 직접 읽음
```

- 재배포 없음 → 배포 이력 깨끗
- R2 는 **전송량(egress) 무료**
- 갱신이 즉시 반영됨

`index.html` 의 `fetch('data.json')` 을 R2 공개 URL 로 바꾸고,
워크플로에서 `aws s3 cp` (R2는 S3 호환) 한 줄을 추가하면 됩니다.
원하시면 이 방식으로 바꿔드리겠습니다.

---

## 주의사항

- **스케줄 워크플로는 저장소가 60일간 활동이 없으면 자동 비활성화**됩니다.
  가끔 커밋하거나, 비활성화 알림 메일이 오면 다시 켜주세요.
- GitHub Actions 크론은 **부하에 따라 몇 분 지연**될 수 있습니다. 정시 보장이 아닙니다.
- 한투 토큰은 24시간 유효하고 발급 횟수 제한이 있어, `.cache` 를 캐시해
  매번 재발급하지 않도록 해뒀습니다.
- 실패해도 자동 재시도하지 않습니다. Actions 탭에서 빨간불을 확인하세요.
